# Notebooks

This directory preserves exploratory notebook material from earlier development stages.

The notebooks remain in the repository for provenance and quick inspection, but they are not the maintained runtime surface. The supported package and script workflow lives under `src/`, `scripts/`, `configs/`, and `tests/`.

GitHub Linguist classification excludes this directory so archived notebooks do not define the repository's primary language breakdown.
